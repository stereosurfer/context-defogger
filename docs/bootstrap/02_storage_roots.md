# Storage Roots

Status: active target storage policy.

```yaml
code_repo: "stereosurfer/context-defogger"
artifact_root: "not used by default"
local_state_root: "not used by default"
private_corpus_root: "not applicable"
```

The repository stores source skill files and governance files only. Do not
commit private source conversations, local test outputs, or generated exports.
